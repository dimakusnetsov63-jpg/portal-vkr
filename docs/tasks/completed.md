# Завершённые задачи

История выполненного, по git-истории. Записи не удаляются — они объясняют,
почему проект устроен так, как устроен.

Хронология изменений в терминах продукта — [`../changelog/CHANGELOG.md`](../changelog/CHANGELOG.md).

---

## Август 2026

### C-3/C-4 — Ограничение частоты входа и отказ от блокировки учётной записи

**Коммиты:** `2ab160f`, `6a916f0`, `c81da87`, `0206a94`, `94afa2b` · **Миграции:** `20260801100000`, `20260802200000` · **Статус:** `DONE`
**Источник:** находки C-3/C-4, [`../AUDIT-2026-07-31.md`](../AUDIT-2026-07-31.md)
**Порядок выката:** [`../ROLLOUT-login-rate-limit.md`](../ROLLOUT-login-rate-limit.md)

Было: `portal_login` доступен анониму без ограничения частоты (аноним мог
одновременно нагружать bcrypt на сервере БД и неограниченно раздувать
`portal_audit_log`), а единственный существовавший троттлинг считал неудачи
**по логину** — значит, зная чужой логин (не секрет, виден в «Команде и
роли»), можно было отключить вход конкретному сотруднику, включая
руководителя.

Стало: ограничение считается **по источнику запроса** (`ip_hash`), а не по
учётной записи — окно отказа растёт экспоненциально с каждой неудачей и
сбрасывается успешным входом; постоянной блокировки нет вовсе. Три фазы
выката, разнесённые по времени намеренно (менять сигнатуру RPC и код,
который её вызывает, одновременно — верный способ временно сломать вход):

1. Таблица `portal_login_attempts`, новая сигнатура `portal_login`
   с параметром `p_ip`, доверенная роль `portal_auth_caller` — добавлены
   без отзыва старых прав, ничего не сломано.
2. Сервер начал подписывать отдельный короткоживущий сервисный JWT
   (`signPortalServiceJwt`, кэш 5 минут, без `sub`/`sid` — не спутать с
   пользовательским токеном) и вызывать auth-RPC под ролью
   `portal_auth_caller`, а не анонимно. IP берётся только из
   `x-forwarded-for` на сервере, никогда из тела запроса.
3. `revoke execute ... from anon` на всех трёх auth-функциях — закрыло
   путь мимо портала, при котором лимит по IP был декоративным (прямой
   вызов PostgREST мог передать `p_ip = null` или любое чужое значение).

Найден и исправлен до применения собственный баг миграции: в черновике
успешный вход не снимал накопленные неудачи, то есть сотрудник,
ошибившийся паролем несколько раз и затем введший его верно, всё равно
упирался бы в отказ при следующей попытке — та же беда, от которой уходили,
просто в другой форме.

Верифицировано на production (отдельного стенда в проекте нет — база одна):
срабатывание лимита и рост `retry_after` (2→4→16 с), привязка к источнику,
а не к логину (другой, ранее не встречавшийся логин с уже ограниченного IP
немедленно получил тот же `429`), независимость другого физического
источника, точное воспроизведение сброса счётчика, и — после фазы 3 —
прямой вызов от `anon` отвергается `42501 permission denied` на всех трёх
функциях, тогда как вызов через приложение продолжает получать обычный
бизнес-ответ.

Не вошло и осталось долгом: истёкшая/отозванная сессия и отключённая
учётка не проверялись отдельным целевым сценарием (сессии в целом работают
исправно); тест «другой источник действительно не блокируется» проводился
с реальным вторым адресом, но не в контролируемых лабораторных условиях.

---

## Июль 2026

### TASK-004 — Встроенная авторизация и управление пользователями

**Статус:** `DONE` (код и миграция; применение к базе — отдельный шаг)
**ТЗ:** [`TASK-004-portal-auth.md`](TASK-004-portal-auth.md)

Supabase Auth заменён собственной системой. Пользователи создаются в портале
(«Настройки → Команда и роли»), пароли лежат bcrypt-хешем в `portal_users`,
сессии — в `portal_sessions` (в базе только sha256 от токена), действия
администратора и события входа — в `portal_audit_log`.

Четыре роли определяют доступ к разделам, и проверка живёт **в базе**:
политики переведены с `using (true)` на `portal_can('<раздел>')`, который
читает роль и активность из таблицы. Отсюда два свойства, которых нельзя было
получить дёшево иначе: смена роли действует со следующего запроса без
повторного входа, а отключение сотрудника обрывает его сессии немедленно.

Данные браузер по-прежнему читает у PostgREST напрямую — портал сам подписывает
короткоживущий JWT секретом проекта. Это сохранило все репозитории и триггеры
аудита нетронутыми ценой одной серверной переменной окружения; альтернативой
был серверный прокси для всего слоя данных.

Удалено: `/forgot-password`, `/update-password`, `lib/supabase/server.ts`,
`lib/supabase/middleware.ts`. Раздел «Настройки» разбит на `sections/settings/`.

### Должности как общее измерение — Этапы 1 и 2

**Коммиты:** `6e4c968`, `ba2cbdd` · **Статус:** `DONE`

Сквозное добавление должности в оба рабочих раздела.

*Этап 1 — справочник и кандидаты:* значение `position` в enum
`candidate_list_type`, засев 21 должности, колонка `candidates.position`,
выбор должности в модалке и карточке, колонка в таблице, фильтр, экспорт CSV.

*Этап 2 — матрица потребности:* три миграции с добавлением `position` и
расширением ограничений уникальности, перенос статуса и комментария на тройку
(проект, город, должность), новый уровень группировки
(`DemandCityGroupRow` + `DemandPositionRow`), фильтр по должности, параметр
`position` в URL, массовое добавление по нескольким должностям с
подтверждением при большом объёме.

Должности намеренно переиспользуют механизм `candidate_list_options`:
пополнение справочника не требует ни миграции, ни релиза.

### Senior-аудит и план до 1.0

**Коммит:** `88e73a5` · **Статус:** `DONE`

Полный технический аудит ([`../AUDIT-2026-07.md`](../AUDIT-2026-07.md)) и план
доведения до версии 1.0 ([`../ROADMAP-1.0.md`](../ROADMAP-1.0.md)).

### История изменений потребности

**Коммит:** `89ffb37` · **Статус:** `DONE`

Таблица `staffing_demand_history`, два триггера `SECURITY DEFINER` и drawer
с историей ячейки. Клиент не может подделать или пропустить запись: у него
нет insert-политики, а `changed_by` заполняется из `auth.uid()` в самой базе.

### Всплывающие элементы поверх липкой матрицы

**Коммит:** `c1d51c4` · **Статус:** `DONE`

Модалки и выпадающие списки внутри матрицы отрисовывались **под** липкой
шапкой и подвалом. Причина — `position: sticky` с `z-index` создаёт
собственный контекст наложения, и увеличение `z-index` вложенного элемента
проблему не решает.

Решение: `createPortal` в `document.body`, для поповеров — координаты из
`getBoundingClientRect()`. Приём описан в
[`../architecture/frontend.md`](../architecture/frontend.md) — повторять его
при добавлении новых всплывающих элементов.

### Статус и комментарий строки потребности

**Коммит:** `8342c2e` · **Статус:** `DONE`

Таблица `staffing_demand_rows`. Запись создаётся только при первом изменении;
её отсутствие означает «активна, без комментария». Обновление одного поля не
затирает второе (`mergeRowMetaPatch`).

### Улучшения работы с матрицей

**Коммит:** `d2d0138` · **Статус:** `DONE`

Правила инлайн-редактирования, массовое добавление, копирование значений,
состояние в URL, навигация по неделям.

### Матрица потребности на реальных данных

**Коммит:** `698e4d7` · **Статус:** `DONE`

Переход раздела с mock-данных на таблицу `staffing_demand`. Осознанное
отличие от остальных таблиц — отказ от soft-delete: очистка ячейки удаляет
строку физически.

### Тесты чистой логики кандидатов

**Коммит:** `7ed90d6` · **Статус:** `DONE`

Vitest на фильтры и метрики. Зафиксировано правило: бизнес-логика живёт в
`.ts`-модулях, потому что только они покрываются тестами.

### Проектная документация

**Коммит:** `1034cd4` · **Статус:** `DONE`

`CLAUDE.md`, `docs/` и README раздела кандидатов.

### Рефакторинг раздела «Кандидаты»

**Коммит:** `76616ed` · **Статус:** `DONE`

Вынос в отдельную папку, отделение чистой логики, разбиение компонента —
без изменения поведения и дизайна. Образец безопасного рефакторинга для
последующих разделов.

### Ребрендинг в «ВКР — Ваш кадровый ресурс»

**Коммит:** `73fda60` · **Статус:** `DONE`

### Метрики кандидатов

**Коммит:** `a23e7bc` · **Статус:** `DONE`

Карточки считаются по текущей отфильтрованной выборке; «ожидают выхода» и
«успешно вышли» взаимоисключающие; пустая выборка даёт нули, а не `NaN`.

### Управляемые справочники

**Коммиты:** `f78795d`, `f553e22` · **Статус:** `DONE`

`candidate_list_options` с порядком и деактивацией + `Combobox`, допускающий
свободный ввод. Именно этот механизм позже переиспользовали должности.

### Аутентификация сотрудников

**Коммиты:** `7beb0d7`, `41e4ade`, `7abd457`, `e509b98` · **Статус:** `DONE`

Вход по email и паролю, восстановление пароля, защита роутов через
`proxy.ts`. Отдельно исправлено чтение `NEXT_PUBLIC_SUPABASE_*` через
статический `process.env.X` — динамический доступ Turbopack не инлайнит в
клиентский бандл.

### Кандидаты на реальных данных

**Коммиты:** `7beb0d7`, `96df77d` · **Статус:** `DONE`

Первый раздел, переведённый с mock-данных на Supabase: таблица `candidates`,
RLS-политики, CRUD, архивирование через `archived_at`.
