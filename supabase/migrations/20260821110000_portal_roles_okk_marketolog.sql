-- Две новые роли портала: ОКК (отдел контроля качества) и Маркетолог.
--
-- Отдельной миграцией и раньше той, что эти значения использует: Postgres
-- не позволяет применить новое значение enum в транзакции, которая его
-- создала, а каждый файл миграции выполняется как одна транзакция. В
-- проекте на это уже напарывались — 20260725100000/20260725100100,
-- 20260805100700/20260805100800 и 20260813100000/20260813100200 разбиты по
-- той же причине (см. docs/database/migrations.md, раздел «Порядок
-- применения»). Строки матрицы прав для новых ролей заводит следующая
-- миграция, 20260821110100.
--
-- Слаги — латиницей, как у четырёх существующих ролей; русские подписи
-- («ОКК», «Маркетолог») живут в ROLE_LABELS в src/lib/auth/roles.ts, в базе
-- их нет.
--
-- Значения дописываются в конец enum. Порядок значений виден только там,
-- где по роли сортируют (`order by p.role` в
-- portal_admin_list_section_permissions), и ни на одно право не влияет:
-- матрица читается по равенству роли, а не по её позиции.

alter type public.portal_user_role add value 'okk';
alter type public.portal_user_role add value 'marketolog';
